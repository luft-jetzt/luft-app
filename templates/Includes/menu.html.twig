<nav class="navbar navbar-expand-md navbar-light bg-light mb-3">
    <a class="navbar-brand" href="{{ path('frontpage') }}">
        Luft<sup class="text-primary">jetzt</sup>
    </a>
    <div class="btn-group">
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <button class="btn btn-primary locate-button d-block d-md-none" type="button" title="Deinen Aufenthaltsort automatisch bestimmen">
            <i class="fa fa-location-arrow"></i>
        </button>
    </div>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

        {{ knp_menu_render('main_menu') }}

        {% if app.request.get('_route') != 'frontpage' %}
            <form method="get" class="form-inline float-right" action="{{ path('display') }}">
                <div class="input-group">
                    <input type="text" class="form-control typeahead" id="nav-query" name="query" placeholder="Suchbegriff, Postleitzahl, Stadtname…" />
                    <div class="input-group-append">
                        <button class="btn btn-primary locate-button d-none d-md-block" type="button" title="Deinen Aufenthaltsort automatisch bestimmen">
                            <i class="fa fa-location-arrow"></i>
                        </button>
                        <button id="search-button" type="submit" class="btn btn-primary" title="Nach deiner Eingabe suchen">
                            <i class="fa fa-search"></i>
                        </button>
                    </div>
                </div>
            </form>
        {% endif %}
    </div>
</nav>
